"""
This is a simple script that prints "Hello World".
"""
def increment(number, by):
    return number + by


print(increment(2, by=3))
#List in python numbers = [1, 2, 3] numbers can be add to it
#Tuple in python numbers = (2, 5) numbers can't be add to it